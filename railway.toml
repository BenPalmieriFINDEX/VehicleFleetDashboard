[build]
builder = "nixpacks"
buildCommand = "npm install && cd server && npm install && npx prisma generate && cd .. && cd client && npm install && npm run build"

[deploy]
startCommand = "PUPPETEER_EXECUTABLE_PATH=$(which chromium) node server/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[environments.production.variables]
NODE_ENV = "production"
